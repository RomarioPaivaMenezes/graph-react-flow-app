{
  "scripts": {
    "start": "ts-node src/server.ts",
    "start-test": "ts-node src/control/sadas.ts",
    "build": "tsc",
    "api-monitor": "ts-node src/monitorStartServer",
    "test": "jest",
    "test:silent": "jest --watchAll --silent --noStackTrace",
    "test:nosilent": "jest --watchAll --verbose true"
  },
  "dependencies": {
    "typescript": "^5.3.3"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.5",
    "express": "^4.18.2",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "ts-node": "^10.9.2"
  }
}
